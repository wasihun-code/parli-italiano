# NPM Package Cleanup Report

The following packages were verified as unused (no runtime, build, dynamic, or test imports) and have been uninstalled from the project:

- `@google/generative-ai`
- `@mlc-ai/web-llm`
- `edge-tts-universal`

The `package.json` and `package-lock.json` have been updated accordingly.
